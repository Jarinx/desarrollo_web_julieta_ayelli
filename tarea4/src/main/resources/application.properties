spring.application.name=tarea4

# MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/tarea4
spring.datasource.username=cc5002
spring.datasource.password=programacionweb

# JPA
# usar schema.sql para crear las tablas
spring.jpa.hibernate.ddl-auto=none 
# mostrar consultas SQL en la consola
spring.jpa.show-sql=true 

# ¿cargar archivos schema.sql y data.sql cada vez?
# spring.sql.init.mode=always
spring.sql.init.mode=never